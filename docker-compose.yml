services:
  php:
    image: php:8.4-fpm
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app-php
    working_dir: /var/www
    ports:
      - 8000:8000
    volumes:
      - ./:/var/www